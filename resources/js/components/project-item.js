export function initProjects(Swiper) {
    const project_item = new Swiper(".project-swiper", {
        pagination: {
            clickable: true,
            el: ".pagination-swiper",
        },
    });
}
